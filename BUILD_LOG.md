# Build Log

## 2026-09-18 — Homepage v1

**Scope:** Full homepage build from `tecknotrove_wireframe.xlsx` (sheet "1. Homepage") and `Tecknotrove Brand Book.pdf`, per user request for an immersive, non-generic, scroll-animated site.

**Stack:** Next.js 16 (App Router, Turbopack) · TypeScript · Tailwind v4 · Lato + JetBrains Mono (next/font/google) · GSAP + ScrollTrigger · Lenis (smooth scroll) · Motion (motion/react) · @phosphor-icons/react.

**Sections built (in order):** Nav (dark, mega-menu overlay) → Hero (full-bleed brand-blue, radar/grid motif, curtain-reveal into next section) → Sector bento grid (Defence/Aviation/Automobile/OESD, asymmetric 4-cell layout) → Stat strip (cream, GSAP count-up) → Why Tecknotrove (parallax full-bleed photo, GSAP scrub) → News & Insights (horizontal scroll-snap gallery) → Partner/Careers (2-tile gradient split) → LinkedIn (60/40 split, dark blue panel) → Callout (orange band) → Footer (black).

**Color pass:** Initially built dark-cinematic; user supplied an explicit section-color legend mid-build (Nav=charcoal, Hero=blue, Stat strip=cream, cards=white/light-grey/light-blue, CTA=orange, Footer=black) plus the brand palette swatch. Re-themed all sections to match via CSS custom properties in `globals.css`; kept dark "islands" (Nav, Footer, the two dark accent panels) as deliberate, brand-book-consistent exceptions to the otherwise light page theme.

**Imagery:** No image-gen credits available (OpenAI/Gemini both quota-exhausted). Client's Aviation/Defence/Driving asset folders were empty OneDrive cloud placeholders; only `OESD/IMAGES` had real photos (used for the OESD sector card and the "Why Tecknotrove" parallax background). Replaced initial `picsum.photos` stock-photo placeholders (News, Partner/Careers) with brand-gradient + Phosphor-icon cards after visual QA caught irrelevant random photos (a dog for "Careers") and per the brand book's explicit ban on staged stock imagery.

**Bug fixed — scroll-reveal reliability:** Motion's `whileInView` left some elements permanently stuck at their hidden `initial` state (confirmed via computed styles, not just screenshots). Replaced with a custom `useInView` hook (native `IntersectionObserver` + a debounced scroll-settle safety net that force-reveals anything the viewport has already reached). Verified via `getComputedStyle` checks under aggressive/fast scroll conditions post-fix. Full detail in memory: `feedback_scroll_reveal_reliability`.

**Verified:** `tsc --noEmit` clean, dev server error log clean, mobile viewport (375px) checked, content order/copy checked via `get_page_text`.

## 2026-09-18 — Interaction pass (buttons, sector fan, folder cards)

**Buttons:** unified every button on the site into `MagneticButton` (nav CTA, hero, callout, floating Enquire). Uiverse-style left-to-right sweep fill on hover, solid brand orange (no gradient). Added a `sweep` prop so buttons on the orange callout band sweep brand blue instead (orange-on-orange was invisible).

**Sector cards:** replaced the bento grid with a scattered row + mousemove "fan" interaction (GSAP): random per-load tilt, elastic staggered scroll-in, hovered card straightens + scales while neighbours' faces slide away proportionally (`22 / (index - active)`), re-scatters on leave.

**Bug fixed — fan appeared dead:** the interaction was gated at `min-width: 768px`, but the preview pane is 732px, so it silently fell back to the stacked mobile layout. Lowered the row + fan to `640px`, tightened card padding/type below `lg`, and hid the card description below `lg` so narrow cards don't get cramped. Also changed the GSAP matchMedia gate from `prefers-reduced-motion: no-preference` (which killed the whole interaction for reduced-motion users) to a conditions object that keeps the interaction but shortens durations and drops the random jitter. Added a global reduced-motion CSS rule collapsing transition/animation durations.

**News cards (folder cards):** rebuilt to match the reference behavior, verified frame-by-frame from a client screen recording (extracted with OpenCV since ffmpeg isn't installed). Correct mechanic: the notched foreground panel **slides down ~120px on hover** to reveal the artwork behind it, the number rides down with the panel, and the title/meta stay anchored to the card bottom (they're a separate absolutely-positioned layer, not inside the panel). Notch is a CSS radial-gradient corner piece, not an image.

**Folder-card shape corrected (second pass):** the notch is NOT a bite out of the top-left corner. Zooming the reference frames 3x showed it is a true **folder tab** — the panel's left ~38% is raised with rounded top corners, then steps down via an S-curve (convex on the tab, concave on the joint) to the lower body which runs to the right edge with a rounded top-right corner. Measured geometry: tab top 20% of card height, body top 28%, tab width 38%, radius 14px, hover drop 22%. Built from three same-colour blocks (tab / concave joint / body) plus a radial-gradient corner for the concave fillet; the whole group translates together on hover while the copy stays pinned to the card bottom.

**Card styling:** blue / grey / black variants, each with an outer frame (`p-[7px]` in a darker tone of the card colour), `aspect-[4/5]`, large number, arrow, title + meta. Row moved into the same `max-w-[1400px] px-5 sm:px-8` container as the heading and switched from a full-bleed horizontal scroller to `grid sm:grid-cols-3`, so left/right padding is now equal instead of left-aligned. Headlines shortened to 2 lines so the number doesn't crowd the copy when the panel drops.

**Folder-tab shape, third pass (final):** the border-radius construction still read as a blocky step. Zooming both the reference and my render 4-7x showed why: quarter-circle corners force the edge to reach fully vertical, leaving a straight vertical wall between the convex and concave corners. The reference has no such wall — its two arcs meet directly at an inflection point, so the step reads as one continuous ~45 degree S-ramp. Replaced the three-div construction with a single SVG `path` (viewBox `0 0 400 500`, `preserveAspectRatio="none"` — safe because the card is locked to `aspect-[4/5]`, so it scales without distortion). Final shape uses a **straight diagonal** for the step (not arcs meeting at an inflection): tab edge runs to x=146, a short quadratic eases into a straight ~50 degree slant from (159,109) to (177,131), then another quadratic eases back onto the body edge at x=190. Two arcs meeting at an inflection still read as "curved"; the reference clearly has a straight slanted segment.

**LinkedIn section rebuilt:** swapped the layout — account details now on the left (label, headline, logo tile + company identity, followers / countries stat row, hashtags, follow button, view-all link), and the two most recent posts on the right as a **stacked polaroid deck**. Polaroid treatment is a white frame with a thick bottom margin, sharp 3px corners (deliberately different from the rounded cards elsewhere so they read as photos), a 4:3 tinted plate per post carrying the category colour and chip, and the post text plus `N days ago · N reactions` in the bottom margin. Cards are absolutely positioned and counter-rotated (-6 / +3 deg); hovering the deck separates and straightens them so both become readable. Below `sm` they fall back to a straight vertical stack with no rotation. Card width capped so the deck doesn't dominate in the single-column range between `sm` and `lg`. Sizes were then scaled up on both sides: polaroids to `sm:w-[78%] sm:max-w-[440px]` (deck height 540px), and the account column to a `text-5xl` headline, 64px logo tile, `text-4xl` stat figures and larger pills/links, with the grid evened to `lg:grid-cols-2`. Per-card hover added on top of the group separation — `hover:z-30` plus `hover:scale-[1.05]` on each card (base `z-10` / `z-20`), so whichever card the cursor is on lifts above the other. Verified by reading computed `z-index` and `scale` during a live hover rather than by eye, since the preview pane's screenshot coordinates don't map 1:1 to viewport pixels.

**Stat strip tightened:** the four figures were spreading across the full 1336px container with ~180px gaps, and the cream band was ~285px tall. Capped the stats block at 980px (gaps down to ~40px), cut section padding from `py-14/18` to `py-8/10`, and moved the "Know more" link from a row underneath to the right-hand end of the stats row (`md:flex-row md:items-center md:justify-between`) — which both fills the empty right side and removes a whole row. Band is now 158px tall, down from ~285px. Below `md` it falls back to the 2x2 grid with the link underneath.

**Closing CTA band revamped:** was a flat centred slogan on a large orange slab with a lot of dead vertical space. Rebuilt as an asymmetric split: pitch on the left (headline broken to two lines, subtext, "Book a demo" + "Download Brochure"), and a dark inset facility card on the right carrying the real contact details from the brand book — registered name, the Mumbai address, phone and website, plus a Mon-Sat / 24x7 row. Padding cut from `py-28/36` to `py-20/24`; the card now fills what used to be empty space rather than the band just being taller. Stacks vertically below `lg`.

**Open item:** "Book a demo" points at `tel:+912261513002` because there is no contact form yet, and the earlier `mailto:info@tecknotrove.com` was an invented address — swap in the real enquiry destination when it exists.

**Real photography sourced from the brochures:** installed PyMuPDF and pulled imagery out of the sector brochure PDFs, which resolves the long-standing "no real photos" gap (the Aviation/Defence/Driving IMAGES folders are empty OneDrive placeholders and image-gen credits are exhausted). Embedded images turned out to be mostly whole brochure pages, so the usable route was to render cover pages at 300 DPI with `get_pixmap` and crop the hero photo band (`y 0.168-0.564`) and the three-up simulator strip (`y 0.573-0.735`) by normalized coordinates. Exported: `sector-defence` (tank, cropped from the Tank Driving spread), `sector-aviation` (cargo high loader), `sector-automobile` (HMV trucks), `sector-oesd` (excavator), plus `news-army` (uniformed operator in a sim), `news-expo` (expo booth rig) and `news-motion` (truck cab on a motion platform). All under 175KB.

Sector cards now use the photo full-bleed behind a tuned light scrim (opaque to 44%, clear by 100%) so the dark copy stays readable while the top half of the photo reads clearly — the earlier flat `via-bg-panel/85` washed the photos out. The abstract grid/sweep/stripe treatments and the per-card `visual` switch are gone. News folder cards drop the Phosphor icon for the photo on the plate, which the sliding panel reveals on hover. Removed the three unused OESD simulation-render JPGs.

**LinkedIn section wired to the real account:** company URL is `linkedin.com/company/tecknotrove-systems-i-pvt-ltd`. Replaced the invented posts with the two most recent real ones (coal-mine training incident cost; operator time-to-competency), each polaroid now deep-linking to its own post permalink in a new tab, with a LinkedIn badge on the plate. Both permalinks were fetched and confirmed to return HTTP 200 with matching post text before going in — worth repeating if they are ever swapped, since a dead permalink is invisible until someone clicks. Follower count corrected from an invented 12,400+ to the verbatim 9,232, company name to the registered "Tecknotrove Systems (I) Pvt Ltd", and the Follow / View-all links point at the real profile instead of `linkedin.com`. Plates use two fresh mining-simulator crops (`li-post-mining`, `li-post-training`) matching the posts' subject matter.

Post dates are shown as month-level ("Sep 2026") rather than the relative "2d / 5d" LinkedIn renders, since relative dates on a static page go stale.

**Rebuilt as authentic LinkedIn post cards (per explicit request):** the polaroid treatment was swapped for a real LinkedIn-style card: profile row (company logo, name, follower count, time, globe icon), post text, the real document cover image, a document title/page-count bar, reaction count, and a Like/Comment/Repost/Send action bar (icons only — no fake counts for comment/repost since neither was visible on the source page). Real post media (the actual 4-page document cover images, not stand-in photos) was pulled by fetching the raw post-page HTML directly with `curl` and grepping for `media.licdn.com` document-cover URLs — the WebFetch-agent route strips `<meta>`/`<img>` tags during its markdown conversion, so og:image and inline image src values don't survive it. Downloaded: `li-post-1.jpg`, `li-post-2.jpg` (real post covers), `li-logo.png` (real company logo, from a `company-logo_100_100` DMS URL). These signed DMS URLs carry an expiry (`e=` param) baked into the querystring, so they had to be downloaded immediately, not just linked.

**Fixed:** the initial stacked layout used a heavy percentage-based overlap (copied from the polaroid version), which is fine for photos but made overlapping post text unreadable, defeating the point of a redirect-through card. Changed to a small fixed-pixel corner-peek (44px/52px offset, `-rotate-1`/`rotate-1`) so both cards are fully readable at rest; hover still separates and lifts the hovered card to the front (z-30, shadow deepens).

**Gotcha:** Tailwind cannot generate utilities from template literals (`group-hover:translate-y-[${SLIDE}%]` silently produces no CSS) — the hover-drop class must be a literal.

**Verified:** `tsc --noEmit` clean, fresh-tab console clean, fan confirmed numerically at both 732px and 1440px, folder-tab geometry confirmed numerically (tab 20%/38%, body 28%) and the slide confirmed visually.

## 2026-09-21 — Partner/Careers + Stat strip revamp

**Partner/Careers:** section had no heading before (naked two-tile grid) — added a mono-label eyebrow + `RevealLines` heading matching the rest of the page. Tiles gained: index number (01/02), a dot-grid texture (radial-gradient, ellipse-masked) replacing the flat radial blob, a top accent bar that scale-x's in on hover, an icon that lifts/rotates on hover, a stat readout (countries / years) in a bottom row, and a real `MagneticButton` CTA in place of the plain text+arrow link, for consistency with the rest of the site's buttons.

**Bug caught mid-build:** first pass kept the outer `FadeUp` as `as="a" href={...}` while nesting a `MagneticButton` (also an anchor) inside it — invalid `<a>`-in-`<a>` HTML, caught via `read_console_messages` (hydration nesting error), not visually. Fixed by dropping the outer anchor; the card is a plain `div` now and the `MagneticButton` is the only real link. Stale console-error caching in the same tab made the fix look like it hadn't landed — opening a fresh tab confirmed it had.

**Stat strip:** added a per-stat Phosphor icon, vertical divider lines between stats (`md:border-l`), a subtle blueprint-grid background (same pattern language as the orange Callout band, left-fade masked), and swapped the plain "Know more" text link for a `MagneticButton` outline for button consistency.

**Verified:** `tsc --noEmit` clean, fresh-tab console clean, both sections screenshotted at 732px including a hover state on the Partner card.

**Partner/Careers, single-viewport pass:** section was taller than a typical 900px viewport (two-line heading, tall cards, generous padding). Collapsed heading to one line, cut card `min-h` 400/460→300/330, padding p-8/10→p-6/8, inner spacing (icon 40→32px, stat figure text-2xl→xl, button py-2.5→2) and section py-24/32→16/20. Measured via `getBoundingClientRect()` on the section rather than eyeballing — confirmed 588px tall at a 1440×900 viewport.

**Sector cards, mobile spacing:** stacked-column gap between the 4 sector cards below `sm` (`gap-4`) read as too loose once the site is checked on an actual phone viewport, not just the 732px preview pane — dropped to `gap-2`. The `sm:` row layout (negative-margin overlap + fan interaction) is untouched.

**Footer, wordmark gradient rebalanced (smaller white band, larger dark fade):** added a middle stop so white is confined to the top 25% and the rest fades down to `0.15` alpha instead of bottoming out at `0.55` — reads as a bright top edge catching light over a mostly-dark letterform.

**Footer, wordmark color, white gradient (final):** previewed a brighter white version (`rgba(255,255,255,0.95)`→`0.55`) locally on request without pushing, client asked to revert to the dark grey, then decided on the white gradient after all — settled and pushed.

**Footer, wordmark color swapped to dark ghost-emboss:** client sent a reference showing the wordmark almost invisible against the black background with a faint top-to-bottom highlight fade (an emboss/pressed look). Swapped the solid muted-blue fill for a gradient-clip text treatment (`rgba(255,255,255,0.12)` top → `rgba(255,255,255,0.02)` bottom, `background-clip: text`) — this is the same technique the very first footer draft used, reapplied here.

**Footer, diamond nudge, corrected:** client asked to nudge left (`right-[-0.1em]`→`0.02em`), then immediately corrected to right instead, then asked for further right again — settled at `right-[-0.14em]`.

**Footer, diamond too far from "E":** the previous no-overlap fix overcorrected — client wanted it touching the corner, not floating clear of it. Pulled back in (`right-[-0.19em]`→`-0.1em`, `top-[-0.14em]`→`-0.03em`, size `0.22em`→`0.2em`), confirmed visually at 1440px: sits right at the letter's top-right corner without clipping into it.

**Footer, diamond overlapping "E":** the right/top offsets that put the mark in the correct general spot still let its bottom-left corner clip into the top of the last letter. Pushed further clear (`right-[-0.06em]`→`-0.19em`, `top-[-0.06em]`→`-0.14em`), confirmed no overlap visually at 1440px.

**Footer, diamond was in the wrong spot entirely:** had placed it above the "T" (start of the word), but the actual nav logo lockup puts it top-right, after the last letter ("Tecknotrove✦"). Confirmed by measuring the source `logo-white.png`'s text-vs-mark bounding boxes directly (scanned the raw RGBA buffer excluding the mark's own region: text spans x 100–1010, mark sits x 980–1104 — i.e. overlapping/after the final "e", floating above the text's cap-height). Re-anchored the mark from `right`/`top` (was `left`) so it sits past the "E", matching the real lockup. Verified at 800px (where the word itself bleeds off-screen, mark still correctly trails whatever letter is currently the visible end) and 1440px (full word visible, mark sits cleanly after "E").

**Footer, diamond mark was clipped/misaligned:** placing the mark as a normal sibling above the wordmark left it floating over empty space, not over the "T" — moved to `position: absolute` sized and positioned in `em` units off a wrapper sharing the wordmark's own responsive font-size classes, so it tracks the text across all 3 breakpoints without per-breakpoint tuning. First attempt was invisible except a sliver: the wrapper still carried `overflow-hidden` (needed to contain the wordmark's `-ml-1` bleed), which was clipping the mark's negative `top` offset above the box. Moved `overflow-hidden` down to wrap only the `<p>`, leaving the absolutely-positioned mark outside its clipping box. Confirmed via `getBoundingClientRect()` at 1440px and visually at both 800px and 1440px widths — mark now sits cleanly above the "T" with a small gap at every size.

**Footer, closed the dead gap + added the logo mark:** the previous pass's top row used no `max-w` wrapper, so on screens wider than 1400px `justify-between` spread the wordmark and the Explore/Follow columns to the literal viewport edges, leaving a large empty gap between them. Wrapped the row in the site's standard `mx-auto max-w-[1400px]` container (same as every other section) so the columns sit right after the content width instead of the raw viewport edge. Also isolated Tecknotrove's orange diamond mark from `logo-white.png` — found its exact pixel bounding box by scanning the raw RGBA buffer for orange-toned pixels (`976,57` to `1104,185`), extracted it with `sharp`, and zeroed the alpha on any non-orange pixel to strip a faint low-alpha decorative artifact that came along with a naive rectangular crop — saved as `logo-mark.png` and placed above the wordmark, matching the nav lockup.

**Footer, redone to match a minimal reference:** client pointed at a specific aesthetic (a Framer footer demo screenshot: one huge solid-color wordmark bleeding off the left edge carrying the whole footer, a quiet 2-column link list top-right, tiny copyright underneath). The 4-column grid + dot-grid texture + top accent bar version was too busy/dense for that direction — replaced it entirely: dropped the logo image, social-icon circles, contact column and background texture; wordmark is now solid (muted steel-blue `rgba(157,177,255,0.55)`, not brand orange — orange is reserved for CTAs, and the reference's desaturated single-tone read felt like the right transferable idea, not its literal color), sized `lg:text-[7.5rem]` with a `-ml-1` crop so it bleeds slightly past the left edge, positioned above a small copyright line. Right side is just "Explore" (5 links) / "Follow" (3 links) in the site's existing `mono-label` eyebrow style. Height dropped to 352px (from 570px). Verified at 1440px and 375px — wordmark bleeds naturally on mobile too, columns stack below it.

**Footer, cut down to size:** first pass rendered ~778px tall — reported as too huge. Wordmark cut roughly in half (`lg:text-[9rem]` → `lg:text-[5rem]`, `19vw`→`13vw` / `11vw`→`7vw` at the smaller breakpoints), and every vertical gap tightened (top `pt-16`→`pt-10`, logo row `pb-14`→`pb-8`, wordmark wrapper `py-6/8`→`py-2/3`, columns row `py-14`→`py-10`, bottom bar `py-8`→`py-6`). Footer height dropped to 570px, confirmed via `getBoundingClientRect()`.

**Footer rebuild:** client compared two Framer footer templates (a graphite/glassmorphism "Sci-Fi HUD" one and a bold-wordmark/minimal "Ondera"-style one) and asked which to use. Recommended neither outright — the HUD/chrome one is a generic AI-startup cliché that clashes with a defence/aviation simulator brand and the site's actual blue/orange palette — but said the bold-wordmark direction was the better instinct. Rebuilt `Footer.tsx` in that spirit without copying either template: kept the existing black footer, added a top orange accent bar + the same faint blueprint-grid texture used elsewhere on the site, an oversized gradient-clipped ghost wordmark ("TECKNOTROVE" at `text-[9rem]`, white 14%→2% top-to-bottom fill) between the logo row and the link columns, and a 4th "Get in Touch" column (phone/site/address, reusing the Callout facility details) alongside the existing 3 link columns. Verified via `getBoundingClientRect()`/computed styles (no horizontal overflow, gradient-clip resolved) after the Browser pane's screenshot capture briefly stalled independent of the code change; confirmed visually once it recovered, at both desktop and 375px widths — the wordmark intentionally bleeds off the mobile edge (`whitespace-nowrap` + `overflow-hidden`), same treatment as the reference.

**Sector cards, scrim was washing out the top of every photo:** the light scrim over each card stayed semi-opaque until 70% up the card height before fading to fully clear only at the very top edge, so ~40% of every photo (right where the sector icon sits) read as a hazy white gap rather than image. Tightened the gradient stops (`0–30%` opaque, clear by `58%`) so the photo is fully visible through the top ~40% of the card and the scrim only covers the bottom text band.

**Sector cards + Why-Tecknotrove imagery, AI-generated via fal.ai:** the brochure-page crops used for the 4 sector cards were low-resolution and generic (whole-page renders cropped down); replaced all 4 with dedicated `fal-ai/flux/dev` generations — operator-POV shots inside each sector's actual simulator cab (tank driving sim w/ curved multi-display, airport pushback-tractor sim, heavy-truck driving sim, excavator/offshore sim), matching the brand book's human-centric, non-stock-photo direction. Also replaced the "Why Tecknotrove" background, which previously just reused the OESD sector photo as a generic wide shot, with a dedicated engineering-workshop image of a motion-platform build (sits under an 88% white overlay in the real page, so the image reads as an ambient texture, not literal content). All generated at `portrait_4_3` (sectors) / `landscape_16_9` (Why), downsized with `sharp` to the actual render size and re-encoded at quality 82 (100–120KB per sector image, 237KB for the wider Why background). Deleted the now-unused `oesd-hero.jpg`. API key provided directly by the client for this purpose; not committed anywhere (used inline via curl, no `.env` needed since generation was a one-off).

**Sector cards, real root cause of the "space after every card":** the previous two fixes (gap-2, pb-12) were real but minor — the actual gap the user kept reporting was ~109px of *empty space baked inside every single card*, not between them. `data-card` (the outer wrapper) used `min-h-[320px]`; `data-card-face` (the visible bordered/bg-panel card) used `h-full`. On `sm:flex-row`, height comes from flex cross-axis stretch against the row, so `h-full` resolves fine. On mobile `flex-col`, height is the main axis — a flex item sized only by `min-height` does not give percentage-height children (`h-full`) a definite reference in Chromium, so the face shrank to its content height (~211px) while the 320px wrapper stayed invisible dead space beneath it, repeated after every card. Confirmed with `getBoundingClientRect()` on both the wrapper and the face before/after. Fix: swapped `min-h-[*]` for an explicit `h-[*]` at every breakpoint (`h-[320px] sm:h-[430px] lg:h-[480px]`), which gives `h-full` a definite parent height to resolve against everywhere. Face height now matches wrapper height (320/320) at all breakpoints tested.

**Sector section, mobile bottom padding:** reported from the live deploy (tecknotrove.vercel.app on an actual phone) as "a lot of space" after the last sector card before the cream stat strip. Root cause: the section's `pb-28` (112px), sized for the single-row desktop layout, was carried unchanged into the 4-card-tall mobile stack, where it reads as dead space rather than breathing room. Split to `pb-12` mobile / `sm:pb-36` desktop — confirmed via `getBoundingClientRect()` that the gap dropped from 112px to 48px at 375px width, `sm:` layout untouched.

**Stat strip, centered numbers:** icon/number/label stack in each of the 4 stat columns was left-aligned by default; switched to `items-center text-center` with symmetric `md:px-4` (dropping the old one-sided `pl-8`) so each stat centres within its column and the divider lines land centred between them.

**Gap between Partner/Careers and LinkedIn sections:** two adjacent large `py-*` values (Partner/Careers `pb-16/20` + LinkedIn `pt-24/32`) stacked into ~230px of dead white space with no visual anchor. Split each section's padding asymmetrically — Partner/Careers `pb-12/16` (top kept at 16/20), LinkedIn `pt-12/16` (bottom kept at 24/32) — closing the gap without touching the spacing either section keeps relative to its other neighbour.

---

**Known follow-ups / open items:**
- Hero, sector cards (Defence/Aviation/Automobile), Why-Tecknotrove background, and News cards currently use abstract/gradient treatments or the one available OESD photo set, not sector-specific real photography — swap in real photos/video once available (client's own asset folders need re-syncing from OneDrive, or image-gen credits need topping up).
- Corporate Video hero button links to the real YouTube link found in the client's asset export (`https://youtu.be/hb-hqyMnMLw`).
- Other wireframe pages (PDP, Defence/Aviation/Driving/OESD sector pages) not yet built.
